{
  "name": "flay",
  "version": "0.7.7",
  "description": "Flay — in-app overlay (snapshot card + admin-only bug reporter + public feature-request board) for React Native / Expo apps.",
  "main": "index.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pera-Labs/flay.git"
  },
  "expo": {
    "plugin": "./app.plugin.js"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-native": ">=0.74.0",
    "@react-native-async-storage/async-storage": ">=1.21.0"
  }
}
